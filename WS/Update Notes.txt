v2 
add json importing feature
fixes the checkparameter function

v3
removed url amount in json and program

v3.5
added testing for dual input, without extra debugging
only 1 seperator supported inside code(not from json) 

v4
added dual input with the errors from 3.5 removed
code improved slightly

added seperator value into json file, 
seperator are contained in a array-like file to support multiple seperator
default seperators are [" "(space), "."(period), ","(coma), "-"(dash), "_"(underscore)]

json file renamed to data.json to avoid confusion

v4.2 
Branch of main Project*
using nicknames. imported from json